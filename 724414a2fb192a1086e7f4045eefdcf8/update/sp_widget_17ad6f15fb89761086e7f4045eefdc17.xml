<?xml version="1.0" encoding="UTF-8"?><record_update table="sp_widget">
    <sp_widget action="INSERT_OR_UPDATE">
        <category>custom</category>
        <client_script><![CDATA[api.controller=function($scope, $rootScope, spUtil) {
    /* widget controller */
    var c = this;
    
    // Certifique-se que c.data é inicializado, especialmente c.data.anoSelecionado
    if (!c.data.anoSelecionado) {
        c.data.anoSelecionado = '';
    }

    // Escuta o evento 'anoSelecionado' enviado pelo outro widget
    var listener = $rootScope.$on('anoSelecionado', function(event, ano) {
        c.data.anoSelecionado = ano; // Armazena o ano ('2025' ou '' para Todos)
        
        // Chama o servidor (Server Script) para recarregar os dados
        c.server.update().then(function() {
            console.log('Regra de Ouro atualizada para o ano:', ano || 'Todos');
        });
    });

    // Limpa o listener quando o escopo do widget é destruído para evitar vazamento de memória
    $scope.$on('$destroy', function() {
        if (listener) {
            listener();
        }
    });
};]]></client_script>
        <controller_as>c</controller_as>
        <css>@import url('https://fonts.googleapis.com/css2?family=Roboto:wght@500&amp;display=swap');
/* --- Variáveis --- */
$ranking-color-initial: #a7b9e8; // Cor inicial do gráfico (azul claro)
$ranking-color-hover: #4b77bf;     // Cor ao passar o mouse (azul escuro)
$ranking-title-color: #021770;     // Cor do título


/* --- Estilos do Título --- */

// Remove o background, border e padding padrão do painel

.panel{
	   border: none !important;
}

/* --- Estilos do Título --- */

// Aplica o espaçamento superior e lateral ao container do título
.panel-heading {
    background-color: transparent;
    border: none;
    padding-top: 15px;    
    padding-left: 20px;   
    padding-right: 0;     
    //margin-bottom: 15px;
}

// Estilos específicos para o Título (h3.panel-title)
.panel-title {
    font-family: 'Roboto', sans-serif;
    font-weight: 500;
    font-size: 24px;
    line-height: 100%;
    color: $ranking-title-color;
    
    /* Remove text-align: center se tiver sido adicionado */
    text-align: left; 
    
    /* Remove qualquer margem extra se não for necessária */
    margin: 0; 
}

/* --- Estilos do Ranking --- */

.ranking-bar-container {
    display: flex;
    align-items: center; // Alinha todos os itens (label, barra, valor) verticalmente
    margin-bottom: 5px;
    height: 25px;
}

.ranking-label {
    font-family: 'Roboto', sans-serif;
    width: 150px;
    font-weight: bold;
    font-size: 14px;
    text-align: right;
    padding-right: 10px;
    /* 🎯 NOVA COR para o Label */
    color: #1d2f80; 
}

.ranking-bar-wrapper {
  	overflow: visible;
    flex-grow: 1; // Ocupa o espaço restante
    height: 100%;
    /* Deixa espaço para o label (150px) e para a porcentagem (cerca de 50px) */
    max-width: calc(100% - 150px - 50px);
    margin-right: 10px; // Espaçamento entre a barra e a porcentagem
}

.ranking-bar {
    height: 80%;
    background-color: $ranking-color-initial;
    border-radius: 2px;
    box-sizing: border-box;
    min-width: 5px;

    /* Transição suave para cor E escala (transform) */
    transition: background-color 0.3s ease, transform 0.3s ease;
    transform: scale(1);
    transform-origin: left; // Faz o crescimento no hover ser da esquerda para a direita
}

/* Efeito Hover: muda a cor do background e aumenta a escala */
.ranking-bar:hover {
    background-color: $ranking-color-hover;
    cursor: pointer;
    transform: scale(1.01); 
}

/* Estilo para o valor da porcentagem (agora fora da barra) */
.ranking-value {
    font-family: 'Roboto', sans-serif;
    /* 🎯 NOVA COR para o Ranking Value */
    color: #1d2f80; 
    font-weight: bold;
    font-size: 14px; 
    width: 40px; // Largura fixa para manter o alinhamento
    text-align: left;
}

// Estilo específico para o item com 100% (se houver necessidade de destaque)
.ranking-bar[ng-style*="'width': 100"] {
    background-color: #3867c2;
    
    &amp;:hover {
        background-color: $ranking-color-hover; 
    }
}


/*----------------------------*/
/* --- Estilos Completos para o Card de Informações --- */

/* 1. Container Principal do Card */
.ranking-info-card {
    border: 1px solid #C8C5C5; 
    border-radius: 4px;
    margin-bottom: 20px; 
    border-top: none; 
}

/* 2. Cabeçalho do Card (Contém o Título) */
.ranking-info-heading {
    /* Cor de fundo solicitada */
    background-color: #F1F4FB; 
    
    /* Borda superior solicitada */
    border-top: 0.5px solid #C8C5C5; 
    
    border-bottom: none; 
    padding: 10px 15px;
}

/* 3. Título (h3) dentro do Cabeçalho */
.ranking-info-title {
    /* Estilos do Título solicitados */
    font-family: 'Roboto', sans-serif;
    font-weight: 600; /* SemiBold */
    font-size: 16px;
    color: #021770; 
    
    /* Propriedades de espaçamento */
    line-height: 100%;
    letter-spacing: 0%;
    vertical-align: bottom;
    
    margin: 0; /* Essencial para remover margens padrão do H3 */
    padding: 0; 
}

/* 4. Corpo do Card */
.ranking-info-body {
    padding: 15px;
    border-top: 1px solid #C8C5C5; /* Separador entre heading e body */
}

/* 5. Parágrafos dentro do corpo */
.ranking-info-body p {
    margin-bottom: 5px; 
    line-height: 100%; 
}

/* 6. Rótulos (Descrição, Fonte, Atualização) - Estilo SemiBold (600) */
.ranking-info-label {
    font-family: 'Roboto', sans-serif;
    font-weight: 600; /* SemiBold */
    font-size: 14px;
    color: #021770; 
    
    line-height: 100%;
    letter-spacing: 0%;
    vertical-align: bottom; 
    margin-right: 5px;
}

/* 7. Conteúdo (Valores: Descrição, Fonte, Atualização) - Estilo Light (300) */
.ranking-info-content {
    font-family: 'Roboto', sans-serif;
    font-weight: 100; /* Light */
    font-size: 12px;
    color: #021770; 
    
    line-height: 100%;
    letter-spacing: 0%;
    vertical-align: bottom; 
    
    font-style: normal;
}</css>
        <data_table>sp_instance</data_table>
        <demo_data/>
        <description/>
        <docs/>
        <field_list/>
        <has_preview>true</has_preview>
        <id>regra_de_ouro_grafico</id>
        <internal>false</internal>
        <link><![CDATA[function link(scope, element, attrs, controller) {
  
}]]></link>
        <name>Regra de Ouro</name>
        <option_schema/>
        <public>true</public>
        <roles/>
        <script><![CDATA[(function() {
    
    data.rankingData = []; // Array para armazenar os dados do gráfico

    // Inicialização segura do filtro de ano
    var anoFiltro = (input && input.anoSelecionado) ? input.anoSelecionado : ''; 
    data.anoSelecionado = anoFiltro;

    // ... (restante do código para obter descrição, fonte e atualização) ...

    var rankingTypeSysId = gs.getProperty('x_sded9_portal_t_0.regra_de_ouro_id') ||'b0e84d38fb857210f6d5f9d38eefdc60';
    
    var grTipo = new GlideRecord('x_sded9_portal_t_0_tipos_de_ranking_area'); 
    if (grTipo.get(rankingTypeSysId)) {
        data.descricao = grTipo.u_descricao.toString();
        data.fonte = grTipo.u_fonte.getDisplayValue();
        data.atualizacao = grTipo.u_atualizacao.toString();
    } else {
        // Fallback caso o registro não seja encontrado
        data.descricao = 'Descrição não disponível.';
        data.fonte = 'Fonte não disponível.';
        data.atualizacao = 'Atualização não disponível.';
    }

    var gr = new GlideRecord('x_sded9_portal_t_0_dados_do_ranking');
    gr.addQuery('u_tipo_de_ranking', rankingTypeSysId);
    
    // Aplica o filtro de ano
    if (anoFiltro) {
        gr.addQuery('u_ano', anoFiltro);
    }
    
    // Adiciona ordenação por ano se for "Todos"
    if (!anoFiltro) {
        gr.orderByDesc('u_ano');
    }
    
    gr.query();

    while (gr.next()) {
        // ... (seu código de processamento de dados para push no data.rankingData) ...
        var estadoNome = gr.u_estado.getDisplayValue();
        var posicaoStr = gr.u_posicao.toString();
        
        var cleanedPosicaoStr = posicaoStr.replace(/\.0/g, '').replace(/°/g, '') + '°'; 
        var posicaoNum = parseInt(cleanedPosicaoStr.replace(/\D/g, ''), 10);
        var porcentagemStr = gr.u_porcentagem.toString(); 
        var cleanedStr = porcentagemStr.replace(/,/g, '.'); 
        var porcentagemParaBarra = parseFloat(cleanedStr); 
        var anoDoRegistro = gr.u_ano.toString();
        
        if (isNaN(porcentagemParaBarra)) {
             porcentagemParaBarra = 0;
        }

        data.rankingData.push({
            nome: estadoNome,
            posicao_str: cleanedPosicaoStr, 
            posicao_num: posicaoNum, 
            porcentagem_num: porcentagemParaBarra,
            ano_registro: anoDoRegistro
        });
    }

    // Ordenação do maior para o menor (27º, 26º... 1º)
    data.rankingData.sort(function(a, b) {
        return b.posicao_num - a.posicao_num; 
    });

    // ]]>🚨<![CDATA[ ADICIONAR ESTA LINHA: Verifica se há dados
    data.noData = data.rankingData.length === 0 && anoFiltro;
    // data.noData será true se o array estiver vazio E um ano específico estiver selecionado
    // Se nenhum ano estiver selecionado (anoFiltro vazio), não exibe a mensagem de 'sem dados'.

})();]]></script>
        <servicenow>false</servicenow>
        <sys_class_name>sp_widget</sys_class_name>
        <sys_created_by>patrick.almeida@vaexpert.com.br</sys_created_by>
        <sys_created_on>2025-11-06 00:34:45</sys_created_on>
        <sys_id>17ad6f15fb89761086e7f4045eefdc17</sys_id>
        <sys_mod_count>9</sys_mod_count>
        <sys_name>Regra de Ouro</sys_name>
        <sys_package display_value="Portal Tesouro" source="x_sded9_portal_t_0">724414a2fb192a1086e7f4045eefdcf8</sys_package>
        <sys_policy/>
        <sys_scope display_value="Portal Tesouro">724414a2fb192a1086e7f4045eefdcf8</sys_scope>
        <sys_update_name>sp_widget_17ad6f15fb89761086e7f4045eefdc17</sys_update_name>
        <sys_updated_by>patrick.almeida@vaexpert.com.br</sys_updated_by>
        <sys_updated_on>2025-11-12 02:36:48</sys_updated_on>
        <template><![CDATA[<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">Regra de Ouro</h3>
    </div>
    <div class="panel-body">
        
        <div ng-if="c.data.noData" class="alert alert-info">
            Não há registro para o ano selecionado ({{c.data.anoSelecionado}}).
        </div>
        
        <div ng-if="!c.data.noData" ng-repeat="item in c.data.rankingData" class="ranking-bar-container">
            
            <div class="ranking-label">
                <span class="ranking-position-label">{{item.posicao_str}}</span>
                {{item.nome}}
                
                <span ng-if="!c.data.anoSelecionado" class="ranking-ano-registro">
                    ({{item.ano_registro}})
                </span>
            </div>
            
            <div class="ranking-bar-wrapper">
                <div class="ranking-bar" ng-style="{ 'width': item.porcentagem_num + '%' }">
                </div>
            </div>
            
        </div>
        
    </div>
</div>

---

<div class="ranking-info-card">
    <div class="ranking-info-heading">
        <h3 class="ranking-info-title">Informações:</h3>
    </div>
    <div class="ranking-info-body">
        <p>
            <strong class="ranking-info-label">Descrição:</strong> 
            <span class="ranking-info-content">{{c.data.descricao}}</span>
        </p>
        <p>
            <strong class="ranking-info-label">Fonte:</strong> 
            <span class="ranking-info-content">{{c.data.fonte}}</span>
        </p>
        <p>
            <strong class="ranking-info-label">Atualização:</strong> 
            <span class="ranking-info-content">{{c.data.atualizacao}}</span>
        </p>
    </div>
</div>

<style>
/* Estilo opcional para diferenciar o ano */
.ranking-ano-registro {
    font-size: 0.85em;
    color: #6c757d; 
    margin-left: 5px;
}

/* Estilo para a mensagem de alerta, apenas para garantir a aparência */
.alert-info {
    padding: 15px;
    margin-bottom: 20px;
    border: 1px solid #b8daff;
    border-radius: 4px;
    color: #0c5460;
    background-color: #d1ecf1;
}
</style>]]></template>
    </sp_widget>
</record_update>
