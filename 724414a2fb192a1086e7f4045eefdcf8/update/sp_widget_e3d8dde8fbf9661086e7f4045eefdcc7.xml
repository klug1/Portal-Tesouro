<?xml version="1.0" encoding="UTF-8"?><record_update table="sp_widget">
    <sp_widget action="INSERT_OR_UPDATE">
        <category>custom</category>
        <client_script><![CDATA[function($scope, $http, $timeout, $uibModal, $sce) {

    $scope.modalData = {};

    $scope.openModal = function(node) {
        var usersHtml = node.users ? $sce.trustAsHtml(node.users) : '';

        $scope.modalData = {
            name: node.name,
            title: node.title,
            desc: node.desc,
            users: usersHtml,
            gestorTitular: node.gestorTitular || 'Não informado',
            gestorSub1: node.gestorSub1 || 'Não informado',
            gestorSub2: node.gestorSub2 || 'Não informado'
        };

        $scope.modalInstance = $uibModal.open({
            templateUrl: 'modalTemplate',
            scope: $scope
        });
    };

    $scope.closeModal = function() {
        if ($scope.modalInstance) {
            $scope.modalInstance.close();
            $scope.modalInstance = null;
        }
    };

    function loadScript(src) {
        return new Promise(function(resolve, reject) {
            var script = document.createElement('script');
            script.src = src;
            script.onload = resolve;
            script.onerror = reject;
            document.head.appendChild(script);
        });
    }

    function loadAllScripts() {
        var scripts = [
            'https://code.highcharts.com/highcharts.js',
            'https://code.highcharts.com/modules/sankey.js',
            'https://code.highcharts.com/modules/organization.js',
            'https://code.highcharts.com/modules/exporting.js',
            'https://code.highcharts.com/modules/accessibility.js'
        ];

        var promise = Promise.resolve();
        for (var i = 0; i < scripts.length; i++) {
            (function(src) {
                promise = promise.then(function() {
                    return loadScript(src);
                });
            })(scripts[i]);
        }
        return promise;
    }

    function initChart() {
        loadAllScripts().then(function() {

            Highcharts.setOptions({
                chart: {
                    style: {
                        fontFamily: 'Roboto Flex, sans-serif'
                    }
                }
            });

            Highcharts.chart('container', {
                chart: {
                    height: 800,
                    inverted: true,
                    events: {
                       render: function() {
                          var parents = document.querySelectorAll('.highcharts-label.highcharts-data-label.highcharts-data-label-color-undefined.highcharts-tracker');
                          parents.forEach(function(parent) {
                            var h4s = parent.querySelectorAll('h4');
                            h4s.forEach(function(h4) {
                              h4.style.display = 'none';
                            });

                            var ps = parent.querySelectorAll('p');
                            ps.forEach(function(p) {
                              p.style.fontSize = '18px';
                            });
                          });
                       }
                    }
                },
                title: {
                    text: null
                },
                series: [{
                    type: 'organization',
                    name: 'Organograma',
                    keys: ['from', 'to'],
                    data: $scope.data.links,
                    nodes: $scope.data.nodes,
                    colorByPoint: false,
                    color: '#293273',
                    borderColor: 'white',
                    nodeWidth: 100,
										borderRadius: 45,
                    dataLabels: {
                        color: 'white',
                        style: {
                            textOutline: 'none',
                            fontWeight: 'bold'
                        },
                        formatter: function() {
                            return this.point.title;
                        }
                    },  states: {
        inactive: {
            opacity: 0.5 // Aumente esse valor para deixar menos transparente
        }
    },
                    events: {
                        click: function(event) {
                            var node = event.point;
                            $scope.openModal({
                                name: node.name,
                                title: node.title,
                                desc: node.desc,
                                users: node.users,
                                gestorTitular: node.gestorTitular,
                                gestorSub1: node.gestorSub1,
                                gestorSub2: node.gestorSub2
                            });
                            $scope.$apply();
                        }
                    }
                }],
                tooltip: {
                    enabled: false
                },
                exporting: {
                    enabled: true,
                    allowHTML: true,
                    sourceWidth: 1000,
                    sourceHeight: 800,
                    buttons: {
                        contextButton: {
                            menuItems: ['printChart', 'viewFullscreen']
                        }
                    }
                }
            });

            // Cantos arredondados dos nós
            var rects = document.querySelectorAll('#container svg rect.highcharts-point');
            for (var j = 0; j < rects.length; j++) {
                rects[j].setAttribute('rx', '15');
                rects[j].setAttribute('ry', '15');
            }

        }).catch(function(e) {
            console.error('Erro ao carregar ou inicializar gráfico:', e);
        });
    }

    $timeout(function() {
        initChart();
    }, 1000);

}
]]></client_script>
        <controller_as>c</controller_as>
        <css>@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&amp;display=swap');

div, h3, h2, h1, h4 {
	 font-family: 'Roboto Flex', sans-serif;
}

/* ---------------------------------------------------------------------- */
/* ESTILOS BASE (APLICADOS EM TODAS AS TELAS - MOBILE FIRST: &lt; 576px)     */
/* ---------------------------------------------------------------------- */
#titulo-organorgama {
    /* Tipografia e Cor */
    font-family: 'Roboto', sans-serif;
    font-weight: 600; /* SemiBold */
    color: #4671C4;

    /* Centralização (Flexbox) */
    display: flex;
    justify-content: center; /* Centraliza Horizontalmente */
    align-items: center;     /* Centraliza Verticalmente */
    text-align: center;
    line-height: 1.2;

    /* Tamanho e Altura para Telas Muito Pequenas (Exemplo: iPhone SE) */
    font-size: 28px;
    height: 150px; /* Altura menor para telas mais estreitas */
}



/* ---------------------------------------------------------------------- */
/* RESPONSIVIDADE (MEDIA QUERIES)                                         */
/* ---------------------------------------------------------------------- */

/* 1. Telas Pequenas (sm: a partir de 576px - a maioria dos celulares) */
@media (min-width: 576px) { 
    /* Usamos '576px' que é o ponto de interrupção 'sm' do Bootstrap */
    #titulo-organorgama {
        font-size: 36px;
        height: 70px;
    }
}

/* 2. Telas Médias (md: a partir de 768px - tablets) */
@media (min-width: $screen-sm-min) {
    #titulo-organorgama {
        font-size: 40px;
        height: 80px;
    }
}

/* 3. Telas Grandes (lg: a partir de 992px - desktops) */
@media (min-width: $screen-md-min) {
    #titulo-organorgama {
        font-size: 48px; /* O tamanho original que você solicitou */
        height: 150px;
    }
}</css>
        <data_table>sp_instance</data_table>
        <demo_data/>
        <description/>
        <docs/>
        <field_list/>
        <has_preview>true</has_preview>
        <id>organograma_tesouro_clone_v2</id>
        <internal>false</internal>
        <link><![CDATA[function link(scope, element, attrs, controller) {
angular.module('sn.$sp').directive('orgChart', function($compile) {
  return {
    scope: {
      node: '='
    },
    templateUrl: 'org-chart-template',
    link: function(scope, element) {
      $compile(element.contents())(scope);
    }
  };
});
	

}]]></link>
        <name>Organograma Tesouro Clone v2</name>
        <option_schema/>
        <public>true</public>
        <roles/>
        <script><![CDATA[(function() {

    // Função para buscar usuários ativos de um departamento
    function getUsersByDepartment(deptSysId) {
        var users = [];
        var userGr = new GlideRecord('sys_user');
        userGr.addQuery('active', true);
        userGr.addQuery('department', deptSysId);
        userGr.query();
        while (userGr.next()) {
            //users.push(userGr.name.toString() + ' - ' + (userGr.title.toString() || 'Sem função'));
					users.push(userGr.name.toString());
        }
        return users;
    }

    // Função para buscar info do usuário (gestor) pelo sys_id
    function getUserInfo(userSysId) {
        if (!userSysId) return 'Não informado';
        var userGr = new GlideRecord('sys_user');
        if (userGr.get(userSysId)) {
          //  return userGr.name.toString() + ' - ' + (userGr.title.toString() || 'Sem função');
					return userGr.name.toString();
        }
        return 'Não encontrado';
    }

    var nodes = [];
    var links = [];

    function fetchChildren(parentSysId, level) {
        var gr = new GlideRecord('cmn_department');
        gr.addQuery('u_nome_unidade_superior', parentSysId);
        gr.query();

        while (gr.next()) {
            var childSysId = gr.sys_id.toString();

            var users = getUsersByDepartment(childSysId);

            var gestorTitular = getUserInfo(gr.getValue('u_matricula_responsavel'));
            var gestorSub1 = getUserInfo(gr.getValue('u_matricula_substituto_1'));
            var gestorSub2 = getUserInfo(gr.getValue('u_matricula_substituto_2'));

            var color = '#6BB7F0'; // Azul escuro para netos e além
            if (level === 0) color = '#4A75B1'; // Cinza para o pai (root)
            else if (level === 1) color = '#4A75B1'; // Azul claro para os filhos diretos

            nodes.push({
                id: childSysId,
                title: gr.getValue('u_sigla_unidade') || '',
                name: gr.name.toString(),
                desc: gr.getValue('description') || '',
                users: users.join('<br>'),
                gestorTitular: gestorTitular,
                gestorSub1: gestorSub1,
                gestorSub2: gestorSub2,
                color: color
            });

            links.push([parentSysId, childSysId]);

            fetchChildren(childSysId, level + 1);
        }
    }

    var rootSysId = '22a5b94487030690947032a70cbb35ae'; // Seu root específico
    var root = new GlideRecord('cmn_department');
    if (root.get(rootSysId)) {
        var users = getUsersByDepartment(rootSysId);

        var gestorTitularRoot = getUserInfo(root.getValue('u_matricula_responsavel'));
        var gestorSub1Root = getUserInfo(root.getValue('u_matricula_substituto_1'));
        var gestorSub2Root = getUserInfo(root.getValue('u_matricula_substituto_2'));

        nodes.push({
            id: rootSysId,
            title: root.getValue('u_sigla_unidade') || '',
            name: root.name.toString(),
            desc: root.getValue('description') || '',
            users: users.join('<br>'),
            gestorTitular: gestorTitularRoot,
            gestorSub1: gestorSub1Root,
            gestorSub2: gestorSub2Root,
            color: '#E66734'
        });
    }

    fetchChildren(rootSysId, 1);

    data.links = links;
    data.nodes = nodes;

})();
]]></script>
        <servicenow>false</servicenow>
        <sys_class_name>sp_widget</sys_class_name>
        <sys_created_by>patrick.almeida@vaexpert.com.br</sys_created_by>
        <sys_created_on>2025-05-25 16:25:06</sys_created_on>
        <sys_id>e3d8dde8fbf9661086e7f4045eefdcc7</sys_id>
        <sys_mod_count>223</sys_mod_count>
        <sys_name>Organograma Tesouro Clone v2</sys_name>
        <sys_package display_value="Portal Tesouro" source="x_sded9_portal_t_0">724414a2fb192a1086e7f4045eefdcf8</sys_package>
        <sys_policy/>
        <sys_scope display_value="Portal Tesouro">724414a2fb192a1086e7f4045eefdcf8</sys_scope>
        <sys_update_name>sp_widget_e3d8dde8fbf9661086e7f4045eefdcc7</sys_update_name>
        <sys_updated_by>patrick.almeida@vaexpert.com.br</sys_updated_by>
        <sys_updated_on>2025-10-31 02:53:44</sys_updated_on>
        <template><![CDATA[<style>
  #container .highcharts-point {
    filter: drop-shadow(0 0 0 white) drop-shadow(0 0 0 white);
    stroke-linejoin: round !important;
    stroke-linecap: round !important;
  }

  #container .highcharts-data-label text {
    font-family: 'Roboto Flex', sans-serif;
  }

  .highcharts-menu {
    font-size: 18px !important;
  }

  .highcharts-a11y-proxy-element .highcharts-no-tooltip {
    font-size: 18px !important;
  }
  
  /* Aumenta a largura do modal */
.panel.panel-default {
  position: fixed;
  left: 50%;
  transform: translateX(-50%);
  max-width: 800px;
  width: 90vw;
  word-wrap: break-word;
  overflow-wrap: break-word;
}

  h4{
  	cursor: pointer;
  }

/* Opcional: para o conteúdo não ficar apertado */
.panel-body.wrapper-xl p {
  white-space: normal !important;
}


  @import url('https://fonts.googleapis.com/css2?family=Roboto+Flex:opsz,wght@8..144,100..1000&display=swap');
</style>

<div id="titulo-organorgama">
  Organograma do Tesouro Estadual
</div>

<div class='containerOrg' id="container" style="min-width: 310px; max-width: 100%; height: 800px; margin: 0 auto"></div>

<!-- Modal Template -->
<script type="text/ng-template" id="modalTemplate">
  <div class="panel panel-default">
    <div class="panel-heading clearfix">
      <h4 class="panel-title pull-left" style="
          font-family: Poppins, sans-serif; 
          font-weight: 600; 
          font-size: 20px;
          color: #4671C4;
          line-height: 1; 
          vertical-align: middle;
      ">Informações do Departamento</h4>
      
      <button type="button" class="close pull-right" aria-label="Fechar" ng-click="closeModal()">
        <span aria-hidden="true" style="color: #4671C4; opacity: 1;">&times;</span>
      </button>
    </div>
    <div class="panel-body wrapper-xl">
      
      <p><b style="
          font-family: Roboto, sans-serif; 
          font-weight: 700; 
          font-size: 18px;
          color: #3F63A8;
          line-height: 1;
          vertical-align: middle;
      ">Nome:</b> <span style="
          font-family: Roboto, sans-serif; 
          font-weight: 400; 
          font-size: 15px;
          color: #3F63A8;
          line-height: 1.2; /* Ajustado para funcionalidade */
          vertical-align: middle;
          text-transform: uppercase;
      ">{{modalData.name}}</span></p>
      
      <p><b style="
          font-family: Roboto, sans-serif; 
          font-weight: 700; 
          font-size: 18px;
          color: #3F63A8;
          line-height: 1;
          vertical-align: middle;
      ">Sigla:</b> <span style="
          font-family: Roboto, sans-serif; 
          font-weight: 400; 
          font-size: 15px;
          color: #3F63A8;
          line-height: 1.2;
          vertical-align: middle;
          text-transform: uppercase;
      ">{{modalData.title}}</span></p>
      
      <p><b style="
          font-family: Roboto, sans-serif; 
          font-weight: 700; 
          font-size: 18px;
          color: #3F63A8;
          line-height: 1;
          vertical-align: middle;
      ">Descrição:</b> <span style="
          font-family: Roboto, sans-serif; 
          font-weight: 400; 
          font-size: 15px;
          color: #3F63A8;
          line-height: 1.2;
          vertical-align: middle;
          text-transform: uppercase;
      ">{{modalData.desc || 'Sem descrição'}}</span></p>
      
      <p><b style="
          font-family: Roboto, sans-serif; 
          font-weight: 700; 
          font-size: 18px;
          color: #3F63A8;
          line-height: 1;
          vertical-align: middle;
      ">Gestor Titular:</b> <span style="
          font-family: Roboto, sans-serif; 
          font-weight: 400; 
          font-size: 15px;
          color: #3F63A8;
          line-height: 1.2;
          vertical-align: middle;
          text-transform: uppercase;
      ">{{modalData.gestorTitular}}</span></p>
      
      <p><b style="
          font-family: Roboto, sans-serif; 
          font-weight: 700; 
          font-size: 18px;
          color: #3F63A8;
          line-height: 1;
          vertical-align: middle;
      ">Gestor Substituto 1:</b> <span style="
          font-family: Roboto, sans-serif; 
          font-weight: 400; 
          font-size: 15px;
          color: #3F63A8;
          line-height: 1.2;
          vertical-align: middle;
          text-transform: uppercase;
      ">{{modalData.gestorSub1}}</span></p>
      
      <p><b style="
          font-family: Roboto, sans-serif; 
          font-weight: 700; 
          font-size: 18px;
          color: #3F63A8;
          line-height: 1;
          vertical-align: middle;
      ">Gestor Substituto 2:</b> <span style="
          font-family: Roboto, sans-serif; 
          font-weight: 400; 
          font-size: 15px;
          color: #3F63A8;
          line-height: 1.2;
          vertical-align: middle;
          text-transform: uppercase;
      ">{{modalData.gestorSub2}}</span></p>
      
      <p><b style="
          font-family: Roboto, sans-serif; 
          font-weight: 700; 
          font-size: 18px;
          color: #3F63A8;
          line-height: 1;
          vertical-align: middle;
      ">Servidores:</b><br>
        <span ng-if="modalData.users" ng-bind-html="modalData.users" style="
          font-family: Roboto, sans-serif; 
          font-weight: 400; 
          font-size: 15px;
          color: #3F63A8;
          line-height: 1.2;
          vertical-align: middle;
          text-transform: uppercase;
      "></span>
        <span ng-if="!modalData.users" style="
          font-family: Roboto, sans-serif; 
          font-weight: 400; 
          font-size: 15px;
          color: #3F63A8;
          line-height: 1.2;
          vertical-align: middle;
          text-transform: uppercase;
      "><i>Sem usuários cadastrados</i></span>
      </p>
    </div>
  </div>
</script>

]]></template>
    </sp_widget>
</record_update>
