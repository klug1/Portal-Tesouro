<?xml version="1.0" encoding="UTF-8"?><record_update table="sp_widget">
    <sp_widget action="INSERT_OR_UPDATE">
        <category>custom</category>
        <client_script><![CDATA[function($scope, $rootScope, spUtil, $timeout) {

  function atualizarIsAnoTodos() {
    $scope.isAnoTodos = !$scope.anoSelecionado || $scope.anoSelecionado === 'Todos';
  }

  $scope.mesPorExtenso = function(mes) {
    var mesesPT = ['Janeiro', 'Fevereiro', 'Março', 'Abril', 'Maio', 'Junho', 'Julho', 'Agosto', 'Setembro', 'Outubro', 'Novembro', 'Dezembro'];
    var mesesEN = ['january', 'february', 'march', 'april', 'may', 'june', 'july', 'august', 'september', 'october', 'november', 'december'];

    if (!mes) return '';
    mes = mes.toString().toLowerCase();

    var mesNumero = parseInt(mes, 10);
    if (!isNaN(mesNumero) && mesNumero >= 1 && mesNumero <= 12) {
      return mesesPT[mesNumero - 1];
    }

    var indice = mesesEN.indexOf(mes);
    if (indice >= 0) {
      return mesesPT[indice];
    }

    return mes;
  }

  $scope.anoSelecionado = null;
  $scope.mesSelecionadoWidget = null;
  $scope.dadosVisiveisMes = null;
  $scope.isFadingMes = false;
  atualizarIsAnoTodos(); // inicializa

  function carregarDados(ano, mes) {
    $scope.isFadingMes = true;

    $timeout(function() {
      var params = {};
      if (ano) params.filtro_ano = ano;
      if (mes) params.filtro_mes = mes;

      if (!mes) {
        $scope.dadosVisiveisMes = null;
        $scope.mesSelecionadoWidget = null;
        $scope.isFadingMes = false;
        return;
      }

      spUtil.get('rcl_ajustado_card_4_mes_ano_anterior', params).then(function(response) {
        if (response.data && response.data.dadosAdj && response.data.dadosAdj.length > 0) {
          $scope.dadosVisiveisMes = response.data.dadosAdj[0];
          $scope.mesSelecionadoWidget = mes;
          $scope.anoSelecionado = response.data.dadosAdj[0].ano;
        } else {
          $scope.dadosVisiveisMes = null;
          $scope.mesSelecionadoWidget = mes;
        }

        $timeout(function() {
          $scope.isFadingMes = false;
        }, 50);
      });
    }, 500);
  }

  carregarDados(null, null);

  $rootScope.$on('anoSelecionado', function(evt, ano) {
    $scope.anoSelecionado = ano;
    atualizarIsAnoTodos(); // <<< atualiza aqui!
    carregarDados(ano, $scope.mesSelecionadoWidget);
  });

  $rootScope.$on('mesSelecionado', function(evt, mes) {
    $scope.mesSelecionadoWidget = mes;
    carregarDados($scope.anoSelecionado, mes);
  });
}
]]></client_script>
        <controller_as>c</controller_as>
        <css>@import url('https://fonts.googleapis.com/css2?family=Roboto+Flex:opsz,wght@8..144,100..1000&amp;display=swap');

/* --- Wrapper Principal (Animação e Layout) --- */
.financial-widget-wrapper {
  font-family: 'Roboto Flex', sans-serif;
  margin-top: 4rem; /* Alinhamento padrão */
  
  /* Lógica de Transição */
  opacity: 1;
  visibility: visible;
  transform: translateY(0);
  transition: opacity 0.5s ease-in-out, transform 0.5s ease-in-out, visibility 0.5s ease-in-out;
}

.financial-widget-wrapper.fade-out {
  opacity: 0;
  visibility: hidden;
  transform: translateY(20px);
}

/* --- Estrutura Visual do Widget --- */
.financial-widget {
  border: 1px solid #ccc;
  border-radius: 4px;
  overflow: hidden;
  background-color: #fff;
}

.indicator-block {
  text-align: center;
  padding: 0;
}

.indicator-header {
  background-color: #EBEDF1;
  color: #021770;
  font-weight: 400;
  padding: 8px 10px;
  font-size: 14px;
  border-bottom: 1px solid #C7C7C7;
  font-family: 'Roboto Flex', sans-serif;
}

.indicator-value {
  color: #021770;
  font-size: 32px;
  font-weight: 400;
  padding: 10px 10px 15px;
  font-family: 'Roboto Flex', sans-serif;
}

/* Estilo para mensagem de "Nenhum dado" */
.indicator-value.small-text {
  font-size: 18px;
  color: #666;
  line-height: 1.5;
  padding: 20px;
}

/* --- Responsividade --- */
@media (max-width: 767px) {
  .financial-widget-wrapper {
    margin-top: 1rem;
  }
  .indicator-value {
    font-size: 24px;
  }
}

@media (min-width: 768px) and (max-width: 1023px) {
  .financial-widget-wrapper {
    margin-top: 2rem;
  }
}

@media (max-width: 350px) {
  .indicator-header {
    font-size: 12px;
  }
  .indicator-value {
    font-size: 20px;
    padding: 6px;
  }
}</css>
        <data_table>sp_instance</data_table>
        <demo_data/>
        <description/>
        <docs/>
        <field_list/>
        <has_preview>true</has_preview>
        <id>rcl_ajustado_card_4_mes_ano_anterior</id>
        <internal>false</internal>
        <link><![CDATA[function link(scope, element, attrs, controller) {  }]]></link>
        <name>rcl ajustado card 4 mes/ano anterior</name>
        <option_schema/>
        <public>true</public>
        <roles/>
        <script><![CDATA[(function() {
	
	
/*function formatarNumero(valor, divisor, sufixo) {
  var resultado = (valor / divisor);
  resultado = resultado.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ".");
  return resultado + ' ' + sufixo;
}*/
		function formatarNumero(valor) {
  if (typeof valor !== 'number') {
    valor = parseFloat(valor);
  }

  return valor.toFixed(2) // fixa duas casas decimais
              .replace('.', ',') // troca ponto decimal por vírgula
              .replace(/\B(?=(\d{3})+(?!\d))/g, '.'); // separador de milhar
}



  // Captura mês e ano do input
  var anoFiltrado = input && input.filtro_ano ? parseInt(input.filtro_ano, 10) : null;
  var mesFiltrado = input && input.filtro_mes ? input.filtro_mes : null;

  // Busca o último ano da base
  var grAno = new GlideRecord('x_sded9_portal_t_0_u_rreo');
  grAno.orderByDesc('u_ano');
  grAno.setLimit(1);
  grAno.query();

  var ultimoAno = null;
  if (grAno.next()) {
    ultimoAno = parseInt(grAno.getValue('u_ano'), 10);
  }

  // Se não tem ano selecionado, usa (últimoAno -1)
  var anoConsulta = anoFiltrado !== null ? anoFiltrado : (ultimoAno !== null ? ultimoAno - 1 : null);

  var gr = new GlideRecord('x_sded9_portal_t_0_u_rreo');
  if (anoConsulta !== null)
    gr.addQuery('u_ano', anoConsulta);
  if (mesFiltrado)
    gr.addQuery('u_mes', mesFiltrado);
  gr.setLimit(1);
  gr.query();

  if (gr.next()) {
    var valor = parseFloat(gr.getValue('adjusted_rreo_fiscal_management_report_2')) || 0;
   // var valorFormatado = (valor >= 1000000000) ? formatarNumero(valor, 1000000000, 'BI') : formatarNumero(valor, 1000000, 'MI');
		var valorFormatado = formatarNumero(valor, 1, '');

    data.dadosAdj = [{
      ano: anoConsulta,
      mes: mesFiltrado,
      valorSomaAdj: valor,
      valorSomaFormatadoAdj: valorFormatado,
      temDadoAdj: true
    }];
  } else {
    data.dadosAdj = [{
      ano: anoConsulta,
      mes: mesFiltrado,
      valorSomaAdj: 0,
      valorSomaFormatadoAdj: '0',
      temDadoAdj: false
    }];
  }
})();
]]></script>
        <servicenow>false</servicenow>
        <sys_class_name>sp_widget</sys_class_name>
        <sys_created_by>patrick.almeida@vaexpert.com.br</sys_created_by>
        <sys_created_on>2025-05-23 22:07:01</sys_created_on>
        <sys_id>3cf39c0cfbf16e10f6d5f9d38eefdc65</sys_id>
        <sys_mod_count>24</sys_mod_count>
        <sys_name>rcl ajustado card 4 mes/ano anterior</sys_name>
        <sys_package display_value="Portal Tesouro" source="x_sded9_portal_t_0">724414a2fb192a1086e7f4045eefdcf8</sys_package>
        <sys_policy/>
        <sys_scope display_value="Portal Tesouro">724414a2fb192a1086e7f4045eefdcf8</sys_scope>
        <sys_update_name>sp_widget_3cf39c0cfbf16e10f6d5f9d38eefdc65</sys_update_name>
        <sys_updated_by>patrick.almeida@vaexpert.com.br</sys_updated_by>
        <sys_updated_on>2025-12-03 02:01:53</sys_updated_on>
        <template><![CDATA[<style>
  @import url('https://fonts.googleapis.com/css2?family=Roboto+Flex:opsz,wght@8..144,100..1000&display=swap');
</style>

<div ng-if="isAnoTodos">

  <div class="financial-widget-wrapper" ng-class="{'fade-out': isFadingMes}">

    <div class="financial-widget" 
         ng-show="dadosVisiveisMes && dadosVisiveisMes.temDadoAdj">
      <div class="indicator-block">
        <div class="indicator-header">
          Soma Receita Ajustada Mês: {{ mesPorExtenso(mesSelecionadoWidget) || '—' }} {{ anoSelecionado || '' }}
        </div>
        <div class="indicator-value">
          R$ {{dadosVisiveisMes.valorSomaFormatadoAdj}}
        </div>
      </div>
    </div>

    <div class="financial-widget mensagem" 
         ng-show="!dadosVisiveisMes || !dadosVisiveisMes.temDadoAdj">
      <div class="indicator-block">
        <div class="indicator-header">
          Soma Receita Ajustada Ano Anterior
        </div>
        <div class="indicator-value small-text">
          <span ng-if="!mesSelecionadoWidget">Selecione um mês para exibir os dados.</span>
          <span ng-if="mesSelecionadoWidget && (!dadosVisiveisMes || !dadosVisiveisMes.temDadoAdj)">
            Nenhum dado disponível para o filtro selecionado.
          </span>
        </div>
      </div>
    </div>

  </div>

</div>]]></template>
    </sp_widget>
</record_update>
