<?xml version="1.0" encoding="UTF-8"?><record_update table="sp_widget">
    <sp_widget action="INSERT_OR_UPDATE">
        <category>custom</category>
        <client_script><![CDATA[function($scope, $rootScope, spUtil, $timeout) {
  $scope.dadosVisiveis = null;
  $scope.isFading = false;

  function carregarDados(ano) {
    $scope.isFading = true;
    $timeout(function() {
      if (ano === '' || ano == null) {
        $scope.dadosVisiveis = null;
        $scope.isFading = false;
        return;
      }
      
      spUtil.get('despesas_porcentagem_ano_executivo', { filtro_ano: ano }).then(function(response) {
        if (response.data && response.data.dados && response.data.dados.length > 0) {
          $scope.dadosVisiveis = response.data.dados[0];
        } else {
          $scope.dadosVisiveis = null;
        }
        $scope.isFading = false;
      });
    }, 500);
  }

  $rootScope.$on('anoSelecionado', function(evt, ano) {
    carregarDados(ano);
  });

  carregarDados(new Date().getFullYear());
}
]]></client_script>
        <controller_as>c</controller_as>
        <css>/* --- 1. Importação da Fonte --- */
@import url('https://fonts.googleapis.com/css2?family=Roboto+Flex:opsz,wght@8..144,100..1000&amp;display=swap');

/* --- 2. Wrapper Principal --- */
.financial-widget-wrapper {
  font-family: 'Roboto Flex', sans-serif;
  margin-top: 4rem; /* Alinhamento padrão */
  
  /* Transições */
  opacity: 1;
  visibility: visible;
  transform: translateY(0);
  transition: opacity 0.5s ease-in-out, transform 0.5s ease-in-out, visibility 0.5s ease-in-out;
}

.financial-widget-wrapper.fade-out {
  opacity: 0;
  visibility: hidden;
  transform: translateY(20px);
}

/* --- 3. Estrutura do Widget --- */
.financial-widget {
  border: 1px solid #ccc;
  border-radius: 4px;
  overflow: hidden;
  background-color: #fff;
}

.indicator-block {
  text-align: center;
  padding: 0;
}

.indicator-header {
  background-color: #EBEDF1; /* Fundo cinza claro */
  color: #021770; /* Azul escuro */
  font-weight: 400;
  padding: 8px 10px;
  font-size: 14px;
  border-bottom: 1px solid #C7C7C7;
  font-family: 'Roboto Flex', sans-serif;
}

/* Ajuste para o ícone */
.indicator-header i {
  margin-right: 5px;
}

.indicator-value {
  color: #021770; /* Azul escuro */
  font-size: 32px;
  font-weight: 400;
  padding: 10px 10px 15px;
  font-family: 'Roboto Flex', sans-serif;
}

.indicator-value.small-text {
  font-size: 18px;
  color: #666;
  line-height: 1.5;
  padding: 20px;
}

/* --- 4. Responsividade --- */

/* Mobile (até 767px) */
@media (max-width: 767px) {
  .financial-widget-wrapper {
    margin-top: 1rem;
  }
  .indicator-value {
    font-size: 24px;
  }
}

/* Tablets (768px até 1023px) */
@media (min-width: 768px) and (max-width: 1023px) {
  .financial-widget-wrapper {
    margin-top: 2rem;
  }
}

/* Dispositivos estreitos */
@media (max-width: 350px) {
  .indicator-header {
    font-size: 12px;
  }
  .indicator-value {
    font-size: 20px;
    padding: 6px;
  }
}</css>
        <data_table>sp_instance</data_table>
        <demo_data/>
        <description/>
        <docs/>
        <field_list/>
        <has_preview>true</has_preview>
        <id>widget_despesa_comprometimento_executivo</id>
        <internal>false</internal>
        <link><![CDATA[function link(scope, element, attrs, controller) {  }]]></link>
        <name>widget despesa comprometimento Executivo</name>
        <option_schema/>
        <public>true</public>
        <roles/>
        <script><![CDATA[(function() {
  function formatarNumero(valor, divisor, sufixo) {
    var resultado = (valor / divisor).toFixed(3); // fixa 3 casas
    // insere separador de milhar com expressão regular
    resultado = resultado.replace(/\B(?=(\d{3})+(?!\d))/g, ".");
    return resultado + ' ' + sufixo;
  }

  var anoFiltrado = (typeof input !== 'undefined' && input.filtro_ano) ? input.filtro_ano : '';
  var gr = new GlideRecord('x_sded9_portal_t_0_u_despesa_pessoal');

  if (anoFiltrado) {
    gr.addQuery('u_ano', anoFiltrado);
  }
	

  gr.orderByDesc('u_ano');
  gr.query();

  var dados = [];
  while (gr.next()) {
    var despesaLiquida = parseFloat(gr.getValue('u_despesa_liquida_pedidos')) || 0;

    var valorFormatado;
    if (despesaLiquida >= 1000000000) {
      valorFormatado = formatarNumero(despesaLiquida, 1000000000, 'BI');
    } else {
      valorFormatado = formatarNumero(despesaLiquida, 1000000, 'MI');
    }

    dados.push({
      ano: gr.getValue('u_ano'),
      despesaLiquida: despesaLiquida,
      receitaCorrente: parseFloat(gr.getValue('u_receita_corrente_liquida')) || 0,
      comprometimento: parseFloat(gr.getValue('comprometimento_executivo')) || 0,
      limiteAlerta: parseFloat(gr.getValue('u_limite_de_alerta')) || 0,
      limitePrudencial: parseFloat(gr.getValue('u_limite_prudencial')) || 0,
      limiteMaximo: parseFloat(gr.getValue('u_limite_maximo')) || 0,
      valorDespesaFormatado: valorFormatado
    });
  }
	
	//gs.addInfoMessage(JSON.stringify(dados))

  data.dados = dados;
})();
]]></script>
        <servicenow>false</servicenow>
        <sys_class_name>sp_widget</sys_class_name>
        <sys_created_by>allex.sousa@artycs.com.br</sys_created_by>
        <sys_created_on>2025-11-05 15:28:02</sys_created_on>
        <sys_id>a280719dfb093610f6d5f9d38eefdc39</sys_id>
        <sys_mod_count>3</sys_mod_count>
        <sys_name>widget despesa comprometimento Executivo</sys_name>
        <sys_package display_value="Portal Tesouro" source="x_sded9_portal_t_0">724414a2fb192a1086e7f4045eefdcf8</sys_package>
        <sys_policy/>
        <sys_scope display_value="Portal Tesouro">724414a2fb192a1086e7f4045eefdcf8</sys_scope>
        <sys_update_name>sp_widget_a280719dfb093610f6d5f9d38eefdc39</sys_update_name>
        <sys_updated_by>patrick.almeida@vaexpert.com.br</sys_updated_by>
        <sys_updated_on>2025-12-04 02:36:31</sys_updated_on>
        <template><![CDATA[<style>
  @import url('https://fonts.googleapis.com/css2?family=Roboto+Flex:opsz,wght@8..144,100..1000&display=swap');
</style>

<div class="financial-widget-wrapper" ng-class="{'fade-out': isFading}">

  <div class="financial-widget" ng-show="dadosVisiveis">
    <div class="indicator-block">
      <div class="indicator-header">
        <!--i class="fa fa-handshake-o" aria-hidden="true"></i--> Comprometimento
      </div>
      <div class="indicator-value">
        {{dadosVisiveis.comprometimento}}%
      </div>
    </div>
  </div>

  <div class="financial-widget mensagem" ng-show="!dadosVisiveis">
    <div class="indicator-block">
      <div class="indicator-header">
        <!--i class="fa fa-handshake-o" aria-hidden="true"></i--> Comprometimento
      </div>
      <div class="indicator-value small-text">
        Nenhum dado disponível para o ano selecionado.
      </div>
    </div>
  </div>

</div>]]></template>
    </sp_widget>
</record_update>
